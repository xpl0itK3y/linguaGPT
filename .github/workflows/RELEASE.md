# Инструкция по созданию релизов

## 🚀 Автоматические релизы через GitHub Actions

Проект настроен для автоматической сборки и публикации релизов при создании тега.

### Как создать релиз:

1. **Обновите версию** (если нужно):
   - В файлах проекта (версия указана в нескольких местах)

2. **Создайте и запушьте тег**:
   ```bash
   # Формат тега: vМАЖОР.МИНОР.ПАТЧ (например: v1.0.0, v1.1.0, v2.0.0)
   git tag -a v1.0.0 -m "Release version 1.0.0"
   git push origin v1.0.0
   ```

3. **GitHub Actions автоматически**:
   - ✅ Запустит сборку для всех платформ (Linux, Windows, macOS)
   - ✅ Создаст артефакты (исполняемые файлы и установщики)
   - ✅ Опубликует релиз на GitHub со всеми файлами

### Что происходит:

1. **Триггер**: При создании тега с префиксом `v` (например `v1.0.0`)
2. **Сборка**: Запускается на трех платформах параллельно:
   - `ubuntu-latest` - для Linux
   - `windows-latest` - для Windows  
   - `macos-latest` - для macOS
3. **Артефакты**: Создаются файлы для каждой платформы
4. **Релиз**: Автоматически создается GitHub Release со всеми файлами

### Результат:

В разделе [Releases](https://github.com/yourusername/linguagpt/releases) появится новый релиз с:
- 📦 **Windows**: `LinguaGPT.exe` (исполняемый файл)
- 📦 **macOS**: `LinguaGPT-*.dmg` (DMG образ с приложением)
- 📦 **Linux**: `linguagpt_*_amd64.deb` (DEB пакет) и `LinguaGPT` (исполняемый файл)

### Проверка статуса:

После создания тега проверьте статус в разделе **Actions** на GitHub:
- Зеленый ✅ = сборка успешна
- Красный ❌ = ошибка сборки (проверьте логи)

### Ручной запуск:

Можно также запустить workflow вручную через GitHub UI:
1. Перейдите в раздел **Actions**
2. Выберите workflow **Build and Release**
3. Нажмите **Run workflow**

### Отладка:

Если сборка не работает:
1. Проверьте логи в разделе **Actions**
2. Убедитесь, что тег имеет формат `v*.*.*`
3. Проверьте, что все зависимости указаны в `requirements.txt`
4. Убедитесь, что `build.py` работает локально

### Семантическое версионирование:

Рекомендуется использовать [Semantic Versioning](https://semver.org/):
- `v1.0.0` - первый стабильный релиз
- `v1.0.1` - патч-релиз (багфиксы)
- `v1.1.0` - минорный релиз (новые функции)
- `v2.0.0` - мажорный релиз (breaking changes)

